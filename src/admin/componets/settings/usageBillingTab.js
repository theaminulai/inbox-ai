/**
 * Settings page — Usage & Billing tab.
 *
 * Entirely read-only and server-rendered (see
 * includes/Templates/settings-usage.php) — there is nothing for this module
 * to do yet. Kept as an explicit init function (matching every other tab)
 * so a future date-range control has an obvious home.
 */

export function initUsageBillingTab() {
	// No interactive behavior yet — the mockup's "This month" control is
	// decorative, and all figures are already correct on page load.
}
